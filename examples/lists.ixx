items = "apple", "banana", "grape"

if items contains "banana"
- say "Found it"
else
- say "No banana"

# string contains also works
message = "Hello World"

if message contains "World"
- say "Found World in the message"
